<Window x:Class="DataBaseEditor.Browser"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataBaseEditor"
        mc:Ignorable="d"
        Title="Browser" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.15*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <WebBrowser x:Name="webReport" MinHeight="350" MinWidth="570" VerticalAlignment="Top" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0"></WebBrowser>
        <Button x:Name="printButton" Content="Print" Click="printButton_Click" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="40 0" Grid.Column="0" Grid.Row="1" />
        <Button x:Name="closeButton" Content="Close" Click="closeButton_Click" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="40 0" Grid.Column="1" Grid.Row="1"/>
    </Grid>
</Window>
