<Window x:Class="DataBaseEditor.AuthWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataBaseEditor"
        mc:Ignorable="d"
        Title="Autorization" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid MinWidth="300" MaxWidth="400" >
        <StackPanel>
            <TextBlock Text="Authorization" FontSize="30" FontWeight="Bold" Margin="50" HorizontalAlignment="Center"/>
            <TextBox x:Name="textBoxLogin" materialDesign:HintAssist.Hint="Enter login" Style="{StaticResource  MaterialDesignFloatingHintTextBox}" Margin="0 0 0 10"/>
            <PasswordBox x:Name="passBox" materialDesign:HintAssist.Hint="Enter pass" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0 0 0 10"/>
            <Grid Margin="0 15 0 0">
                <Button x:Name="signInButton" Content="Sign in" Click="signInButton_Click" HorizontalAlignment="Left" Padding="40 0" />
                <Button x:Name="signUpButton" Content="Sign up" Click="signUpButton_Click" HorizontalAlignment="Right" Padding="40 0"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
